baseurl: ""

sass:
    sass_dir: styles
    style: compressed

exclude: [vendor]

collections:
  books:
    output: true
    permalink: /:path/

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"
      body_class: "default"